<span class="vcard">
  <a href="/<%= h friend.username %>" class="url" rel="contact" title="<%= h friend.name %>"><img alt="<%= h friend.name %>" class="photo fn" height="24" src="<%= friend.avatar.url %>" width="24"/></a>
</span>
