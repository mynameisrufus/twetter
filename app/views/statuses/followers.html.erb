<% setup(:title=>"People who follow #{current_user.username}", :body_id=>"followers", :body_classes=>['followers']) %>

<div id="followers">

    <h2>Your <%= current_user.followers_count %> Followers</h2>
    <br />

    <table class="followers-table doing" cellspacing="0">
          <%= render :partial=>'user/user', :collection=>current_users %>
    </table>
</div>
