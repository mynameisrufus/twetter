<% setup(:title=>"People #{current_user.username} follows", :body_id=>"followers", :body_classes=>['friends']) %>

<div id="followers">

    <h2>You follow <%= current_user.friends_count %> people</h2>
    <br />

    <table class="followers-table doing" cellspacing="0">
          <%= render :partial=>'user/user', :collection=>current_users %>
    </table>
</div>
